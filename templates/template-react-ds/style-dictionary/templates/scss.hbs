{{#each properties}}
  {{!-- this === space --}}
/**
  * @tokens {{@key}}
*/
  {{#each this}}
    {{#and (isObject this)}}
      {{!-- this === ref --}}
      {{#each this}}
      {{!-- this === 20 --}}
      {{!-- this.prop === values --}}
      {{!-- this.value === 8px --}}
$dy-{{this.name}}: {{{this.value}}}; // Original value {{{this.original.value}}}
        {{#if @last}}

        {{/if}}
      {{/each}}
    {{/and}}
  {{/each}}
{{/each}}