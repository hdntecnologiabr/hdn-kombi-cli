import * as React from "react"

import { {{ capitalize componentName }} } from "./{{pascalCase componentName}}"

export default {
  title: 'Components/{{ capitalize componentName }}',
}

export const Default = () => <{{ capitalize componentName }} />
