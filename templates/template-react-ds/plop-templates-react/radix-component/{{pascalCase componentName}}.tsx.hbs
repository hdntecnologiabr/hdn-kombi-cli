import * as React from "react";

import { ComponentProps } from '@delaunay-ui/web'

import * as Radix{{ capitalize componentName }} from '@radix-ui/react-{{dashCase componentName}}'

export type {{ capitalize componentName }}BaseProps = ComponentProps<typeof Radix{{ capitalize componentName }}.Root>

export interface {{ capitalize componentName }}Props extends {{ capitalize componentName }}BaseProps {
  children: React.ReactNode
}

export const {{ capitalize componentName }} = ({ children, ...props }: {{ capitalize componentName }}Props) => {
  return (
    <Radix{{capitalize componentName}}.Root {...props}>
      {children}
    </Radix{{capitalize componentName}}.Root>
  )
}



